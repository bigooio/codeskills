#!/usr/bin/env node

const { execSync } = require('child_process')
const fs = require('fs')
const path = require('path')
const https = require('https')
const http = require('http')

// 配置
const DEFAULT_BRANCH = 'develop'

// GitHub
const GH = {
  owner: 'bigooio',
  repo: 'codeskills',
  repoUrl: 'https://github.com/bigooio/codeskills.git',
  rawUrl: (branch, file) => `https://raw.githubusercontent.com/${GH.owner}/${GH.repo}/${branch}/${file}`,
  apiUrl: (path_) => `https://api.github.com/repos/${GH.owner}/${GH.repo}/contents/${path_}?ref=${DEFAULT_BRANCH}`
}

// GitCode (国内加速)
const GC = {
  owner: 'codeskills',
  repo: 'codeskills',
  repoUrl: 'https://gitcode.com/codeskills/codeskills.git',
  rawUrl: (branch, file) => `https://gitcode.com/codeskills/codeskills/raw/${branch}/${file}`,
  apiUrl: (path_) => `https://gitcode.com/api/v1/repos/codeskills/codeskills/contents/${path_}?ref=${DEFAULT_BRANCH}`
}

// 当前使用的源
let SOURCE = GH

// 颜色输出
const colors = {
  green: '\x1b[32m',
  blue: '\x1b[34m',
  yellow: '\x1b[33m',
  red: '\x1b[31m',
  cyan: '\x1b[36m',
  reset: '\x1b[0m',
  gray: '\x1b[90m',
  white: '\x1b[37m'
}

function log(color, prefix, message) {
  console.log(`${color}${prefix}${colors.reset} ${message}`)
}

function info(msg) { log(colors.blue, 'ℹ', msg) }
function success(msg) { log(colors.green, '✓', msg) }
function warn(msg) { log(colors.yellow, '⚠', msg) }
function error(msg) { log(colors.red, '✗', msg) }
function header(msg) { log(colors.cyan, '▸', msg) }

function downloadFile(url, headers) {
  return new Promise((resolve, reject) => {
    const request = url.startsWith('https') ? https : http
    let data = ''

    const options = { headers: headers || {} }
    request.get(url, options, (response) => {
      if (response.statusCode === 301 || response.statusCode === 302) {
        downloadFile(response.headers.location, headers).then(resolve).catch(reject)
        return
      }
      response.on('data', chunk => data += chunk)
      response.on('end', () => resolve(data))
    }).on('error', reject)
  })
}

const GH_HEADERS = { 'User-Agent': 'codeskills-cli', 'Accept': 'application/json' }

async function listRemoteSkills() {
  try {
    info('获取远程 Skills 列表...')

    // 尝试 GitHub
    let data = await downloadFile(SOURCE.apiUrl('skills'), GH_HEADERS)
    let skills = []

    if (data && !data.includes('404')) {
      try {
        const files = JSON.parse(data)
        skills = files.filter(f => f.type === 'dir')
      } catch {}
    }

    // GitHub 失败，尝试 GitCode
    if (skills.length === 0 && SOURCE !== GC) {
      SOURCE = GC
      info('GitHub 获取失败，切换到 GitCode...')
      try {
        data = await downloadFile(SOURCE.apiUrl('skills'), {})
        if (data) {
          const files = JSON.parse(data)
          skills = files.filter(f => f.type === 'dir')
        }
      } catch {}
      SOURCE = GH // 重置为 GitHub（后续下载用）
    }

    if (skills.length === 0) {
      warn('无法获取 Skills 列表，请检查网络')
      return
    }

    console.log(`\n${colors.cyan}╔═══════════════════════════════════════════╗${colors.reset}`)
    console.log(`${colors.cyan}║     CodeSkills 远程 Skills${colors.reset}`)
    console.log(`${colors.cyan}╚═══════════════════════════════════════════╝${colors.reset}\n`)

    skills.forEach((skill, i) => {
      console.log(`  ${colors.green}${i + 1}.${colors.reset} ${colors.white}${skill.name}${colors.reset}`)
      console.log(`     ${colors.gray}安装: codeskills install ${skill.name}${colors.reset}\n`)
    })

    console.log(`${colors.yellow}共 ${skills.length} 个 Skills${colors.reset}\n`)

  } catch (e) {
    warn('获取列表失败: ' + e.message)
  }
}

async function cloneAndShow() {
  const tempDir = path.join(__dirname, '..', '..', 'temp-clone')

  try {
    execSync(`git clone --depth 1 -b ${DEFAULT_BRANCH} ${GC.repoUrl} "${tempDir}"`, { stdio: 'pipe' })

    const skillsPath = path.join(tempDir, 'skills')
    if (fs.existsSync(skillsPath)) {
      const skills = fs.readdirSync(skillsPath).filter(f =>
        fs.statSync(path.join(skillsPath, f)).isDirectory()
      )

      console.log(`\n${colors.cyan}╔═══════════════════════════════════════════╗${colors.reset}`)
      console.log(`${colors.cyan}║     CodeSkills Skills${colors.reset}`)
      console.log(`${colors.cyan}╚═══════════════════════════════════════════╝${colors.reset}\n`)

      skills.forEach((skill, i) => {
        console.log(`  ${colors.green}${i + 1}.${colors.reset} ${colors.white}${skill}${colors.reset}`)
      })

      console.log(`\n${colors.yellow}共 ${skills.length} 个 Skills${colors.reset}\n`)
    }

  } finally {
    // 清理临时目录
    execSync(`rm -rf "${tempDir}"`, { stdio: 'pipe' })
  }
}

// 获取目录下所有文件的 Contents API
async function getTree(src, skillName) {
  const s = src || SOURCE
  try {
    const data = await downloadFile(s.apiUrl(`skills/${skillName}`), s === GH ? GH_HEADERS : {})
    return data ? JSON.parse(data) : null
  } catch (e) {
    return null
  }
}

// 递归下载目录内容，支持 GitHub/GitCode
async function downloadSkill(src, skillName) {
  const s = src || SOURCE
  const branch = DEFAULT_BRANCH
  const label = s === GH ? 'GitHub' : 'GitCode'

  info(`从 ${label} 下载 Skill: ${skillName}`)

  const items = await getTree(s, skillName)
  if (!items || !Array.isArray(items)) {
    // 降级：只下载 SKILL.md
    const rawUrl = s.rawUrl(branch, `skills/${skillName}/SKILL.md`)
    const content = await downloadFile(rawUrl)
    if (!content || content.includes('404')) {
      return null
    }
    const skillDir = path.join(process.cwd(), 'skills', skillName)
    execSync(`mkdir -p "${skillDir}"`)
    fs.writeFileSync(path.join(skillDir, 'SKILL.md'), content)
    return 'partial'
  }

  const skillDir = path.join(process.cwd(), 'skills', skillName)
  execSync(`mkdir -p "${skillDir}"`)

  let downloaded = 0
  for (const item of items) {
    if (item.type === 'file') {
      const rawUrl = s.rawUrl(branch, `skills/${skillName}/${item.name}`)
      const content = await downloadFile(rawUrl)
      if (content) {
        fs.writeFileSync(path.join(skillDir, item.name), content)
        downloaded++
      }
    } else if (item.type === 'dir') {
      execSync(`mkdir -p "${path.join(skillDir, item.name)}"`)
      const subItems = await downloadFile(
        s.apiUrl(`skills/${skillName}/${item.name}`),
        s === GH ? GH_HEADERS : {}
      )
      const subFiles = subItems ? JSON.parse(subItems) : []
      for (const sub of subFiles) {
        if (sub.type === 'file') {
          const rawUrl = s.rawUrl(branch, `skills/${skillName}/${item.name}/${sub.name}`)
          const content = await downloadFile(rawUrl)
          if (content) {
            fs.writeFileSync(path.join(skillDir, item.name, sub.name), content)
            downloaded++
          }
        }
      }
    }
  }

  return downloaded > 0 ? 'full' : 'partial'
}

async function installSkill(skillName) {
  info(`安装 Skill: ${skillName}`)

  const parentSkillsDir = path.join(process.cwd(), 'skills')
  if (fs.existsSync(parentSkillsDir)) {
    const skillDir = path.join(parentSkillsDir, skillName)
    if (fs.existsSync(skillDir)) {
      success(`Skill "${skillName}" 已存在`)
      return
    }
  }

  try {
    // 优先 GitHub，失败则 GitCode
    let result = await downloadSkill(GH, skillName)
    if (!result) {
      info('GitHub 下载失败，切换到 GitCode...')
      result = await downloadSkill(GC, skillName)
    }
    if (result) {
      const skillDir = path.join(process.cwd(), 'skills', skillName)
      success(`Skill "${skillName}" 安装成功！`)
      console.log(`\n📁 位置: ${skillDir}`)
    } else {
      error(`Skill "${skillName}" 不存在`)
    }
  } catch (e) {
    error(`安装失败: ${e.message}`)
  }
}

async function searchSkills(query) {
  info(`搜索 Skills: ${query}`)
  try {
    // 优先 GitHub，失败则 GitCode
    let rawUrl = GH.rawUrl(DEFAULT_BRANCH, 'data/skills.json')
    let data = await downloadFile(rawUrl)
    if (!data || data.includes('404')) {
      rawUrl = GC.rawUrl(DEFAULT_BRANCH, 'data/skills.json')
      data = await downloadFile(rawUrl)
    }
    if (data && !data.includes('404')) {
      const skills = JSON.parse(data)
      const q = query.toLowerCase()
      const results = skills.filter(s =>
        s.title.toLowerCase().includes(q) ||
        s.description.toLowerCase().includes(q) ||
        (s.tags || []).some(t => t.toLowerCase().includes(q))
      )
      if (results.length === 0) {
        warn(`没有找到包含 "${query}" 的 Skills`)
        return
      }
      console.log(`\n${colors.green}找到 ${results.length} 个 Skills:${colors.reset}\n`)
      results.forEach((s, i) => {
        console.log(`  ${colors.green}${i + 1}.${colors.reset} ${colors.white}${s.slug}${colors.reset}`)
        console.log(`     ${colors.gray}${s.description.substring(0, 80)}${colors.reset}\n`)
      })
      console.log(`${colors.cyan}安装: codeskills install <name>${colors.reset}`)
    } else {
      // 降级：列出所有
      await listRemoteSkills()
    }
  } catch (e) {
    warn('搜索失败，请使用 codeskills list 查看所有 Skills')
  }
}

function showHelp() {
  console.log(`
${colors.cyan}
╔═══════════════════════════════════════════╗
║         CodeSkills CLI v2.0.0            ║
║      发现编程超能力 - AI Agent 技能工具   ║
╚═══════════════════════════════════════════╝
${colors.reset}

${colors.white}使用方法:${colors.reset}

  ${colors.green}codeskills${colors.reset}               # 显示帮助
  ${colors.green}codeskills list${colors.reset}          # 列出所有可用 Skills
  ${colors.green}codeskills search <关键词>${colors.reset} # 搜索 Skills
  ${colors.green}codeskills install${colors.reset}       # 安装所有 Skills
  ${colors.green}codeskills install <name>${colors.reset} # 安装指定 Skill
  ${colors.green}codeskills update${colors.reset}        # 更新 Skills

${colors.cyan}示例:${colors.reset}

  ${colors.gray}# 查看所有可用的 Skills
  codeskills list

  # 搜索包含关键词的 Skills
  codeskills search python

  # 安装所有 Skills 到当前目录
  codeskills install

  # 安装指定的 Skill
  codeskills install git

  # 更新到最新版本
  codeskills update${colors.reset}
`)
}

async function Command() {
  const args = process.argv.slice(2)
  const command = args[0] || 'help'
  const skillName = args[1]
  const invokedAs = path.basename(process.argv[1])

  console.log(`
${colors.cyan}
╔═══════════════════════════════════════════╗
║         CodeSkills CLI v2.0.0            ║
║      发现编程超能力 - AI Agent 技能工具   ║
╚═══════════════════════════════════════════╝
${colors.reset}
`)

  switch (command) {
    case 'list':
    case 'ls':
      await listRemoteSkills()
      break

    case 'search':
    case 's':
      if (!skillName) {
        warn('请提供搜索关键词，例如: codeskills search python')
      } else {
        await searchSkills(skillName)
      }
      break

    case 'install':
    case 'i':
      if (skillName) {
        await installSkill(skillName)
      } else {
        info('安装所有 Skills...')
        // 完整克隆
        const targetDir = path.join(process.cwd(), 'skills')
        execSync(`mkdir -p "${targetDir}"`)
        try {
          execSync(`git clone --depth 1 -b ${DEFAULT_BRANCH} ${GC.repoUrl} temp-cs`, { stdio: 'pipe', cwd: process.cwd() })
          execSync(`cp -r temp-cs/skills/* "${targetDir}/"`, { stdio: 'pipe' })
          execSync(`rm -rf temp-cs`, { stdio: 'pipe' })
          const skills = fs.readdirSync(targetDir).filter(f => fs.statSync(path.join(targetDir, f)).isDirectory())
          success(`安装完成！共 ${skills.length} 个 Skills`)
          console.log(`\n📁 位置: ${targetDir}`)
          console.log(`\n${colors.gray}查看: cd skills && ls${colors.reset}`)
        } catch (e) {
          error('安装失败')
        }
      }
      break

    case 'update':
    case 'u':
      info('更新 Skills...')
      execSync(`rm -rf "${path.join(process.cwd(), 'skills')}"`, { stdio: 'pipe' })
      await Command() // 重新执行 install
      break

    case 'help':
    case '--help':
    case '-h':
    default:
      if (command !== 'help' && command !== '--help' && command !== '-h') {
        error(`未知命令: ${command}\n`)
      }
      showHelp()
  }
}

Command()
